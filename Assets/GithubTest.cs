using UnityEngine;

public class GithubTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("깃 허브 잘 작동되나요.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
